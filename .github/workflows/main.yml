name: MacOS VNC

on: [push, workflow_dispatch]

jobs:
  vnc:
    runs-on: macos-latest

    steps:
      - name: Download ngrok
        run: curl -sL https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-darwin-arm64.zip -o ngrok.zip

      - name: Extract
        run: unzip ngrok.zip

      - name: Auth ngrok
        run: ./ngrok authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
        env:
          NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}

      - name: Enable VNC Screen Sharing (auth error исправлен!)
        run: |
          echo "🔧 Включаем Screen Sharing + VNC..."

          # Задаём пароль пользователю runner (sysadminctl — родной инструмент macOS)
          sudo sysadminctl -resetPasswordForUser runner -newPassword "P@ssw0rd!"

          # Включаем VNC с legacy-режимом (обязательно для RealVNC Viewer)
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate -configure -access -on \
            -users runner -privs -all -allowAccessFor -allUsers \
            -clientopts -setvnclegacy -vnclegacy yes \
            -clientopts -setvncpw -vncpw "P@ssw0rd!" \
            -restart -agent

          # Perl-хак для правильного хэширования VNC-пароля (решает 99% ошибок аутентификации на macOS 13+)
          echo "P@ssw0rd!" | perl -we '
            BEGIN { @k = unpack "C*", pack "H*", "1734516E8BA8C5E2FF1C39567390ADCA" };
            $_ = <>; chomp; s/^(.{8}).*/$1/;
            @p = unpack "C*", $_;
            foreach (@k) { printf "%02X", $_ ^ (shift @p || 0) };
            print "\n"
          ' | sudo tee /Library/Preferences/com.apple.VNCSettings.txt > /dev/null

          # Дополнительно запускаем сервис
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null || true
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement VNCAlwaysStartOnConsole -bool true

          echo "✅ VNC успешно включён!"
          echo "👤 Username: runner"
          echo "🔑 Password: P@ssw0rd!"

      - name: Create ngrok Tunnel
        run: ./ngrok tcp 5900
